<!DOCTYPE html>
<%= stylesheet_link_tag "accounts.css" %>
<html>
  <head>
    <title>Accounts</title>
  </head>
  <body>
    <%=  link_to('Edit Personal Details', edit_customer_path(current_user)) %>
    <%=  link_to('Create a new account', new_account_path) %>
    <div>
      <h2><B>Accounts</B></h2>
      <br>
      <div class="table-responsive", style="overflow: scroll">
        <table class="table">
          <tbody>
            <% @accounts.each do |account| %>
            <tr>
              <td>
                <h3><B><%= account.accountName %></B></h3>
                <p><%= "Account Number: #{account.accountNumber}" %></p>
                <p><%= formatBalance(account) %></p>
              </td>
              <td style="align: left">
                <%= link_to 'View Transactions', transactions_path(account_id: account.id), class: "btn btn-primary", style: "width: 100%; margin: 2%; padding:100 0 100 0" %>
                <br>
                <%= link_to 'Make Payment', new_transaction_path(account_id: account.id), class: "btn btn-primary", style: "width: 100%; margin: 2%" %>
              </td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </body>
</html>
