<%=  link_to('Home', customer_login_url)%>

<%=  form_for(@customer, :method => 'post') do |f| %>

    <%= render(:partial => 'form', :locals => {:f => f}) %>

<%= f.submit("Save Changes") %>
<% end %>
